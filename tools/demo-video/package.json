{
  "name": "@fc-app/demo-video",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Records click-through videos of the app. Deliberately outside the npm workspaces — see README.",
  "scripts": {
    "record": "node src/run.mjs",
    "flows": "node src/run.mjs --list"
  },
  "dependencies": {
    "ffmpeg-static": "^5.3.0",
    "playwright": "^1.62.1"
  }
}
