/**
 * Kysely database schema types.
 *
 * Each table gets an interface here and a corresponding entry in `Database`.
 * The interface is intentionally empty until the first domain migration lands
 * (see docs/product/product-spec.md for the planned data model).
 */
// eslint-disable-next-line @typescript-eslint/no-empty-object-type
export interface Database {}
